// Must be strings instead of symbols, because they are used as
// values in the DOM and in the URL of the referrer calls.
const REFERRERS_SORTING_TOP = 'top'
const REFERRERS_SORTING_RECENT = 'recent'

module.exports = {
	REFERRERS_SORTING_TOP,
	REFERRERS_SORTING_RECENT
}