// Constants will be shared between client and server.
// They will be used as values in the DOM and in the URL of the referrer calls.
const OS_SORTING_TOP = 'top'
const OS_SORTING_RECENT = 'recent'
const OS_TYPE_WITH_VERSION = 'withVersion'
const OS_TYPE_NO_VERSION = 'noVersion'

module.exports = {
	OS_SORTING_TOP,
	OS_SORTING_RECENT,
	OS_TYPE_WITH_VERSION,
	OS_TYPE_NO_VERSION
}