// Constants will be shared between client and server.
// They will be used as values in the DOM and in the URL of the referrer calls.
const EVENTS_TYPE_ACTIONS = 'actions'
const EVENTS_TYPE_CATEGORIES = 'categories'
const EVENTS_TYPE_COMBINED = 'combined'

module.exports = {
	EVENTS_TYPE_ACTIONS,
	EVENTS_TYPE_CATEGORIES,
	EVENTS_TYPE_COMBINED
}