// Constants will be shared between client and server.
// They will be used as values in the DOM and in the URL of the referrer calls.
const COUNTRIES_SORTING_TOP = 'top'
const COUNTRIES_SORTING_RECENT = 'recent'

module.exports = {
	COUNTRIES_SORTING_TOP,
	COUNTRIES_SORTING_RECENT
}