// Constants will be shared between client and server.
// They will be used as values in the DOM and in the URL of the views calls.
const VIEWS_TYPE_UNIQUE = 'unique'
const VIEWS_TYPE_TOTAL = 'total'
const VIEWS_INTERVAL_DAILY = 'daily'
const VIEWS_INTERVAL_MONTHLY = 'monthly'
const VIEWS_INTERVAL_YEARLY = 'yearly'

module.exports = {
	VIEWS_TYPE_UNIQUE,
	VIEWS_TYPE_TOTAL,
	VIEWS_INTERVAL_DAILY,
	VIEWS_INTERVAL_MONTHLY,
	VIEWS_INTERVAL_YEARLY
}