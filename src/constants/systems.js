// Constants will be shared between client and server.
// They will be used as values in the DOM and in the URL of the referrer calls.
const SYSTEMS_SORTING_TOP = 'top'
const SYSTEMS_SORTING_RECENT = 'recent'
const SYSTEMS_TYPE_WITH_VERSION = 'withVersion'
const SYSTEMS_TYPE_NO_VERSION = 'noVersion'

module.exports = {
	SYSTEMS_SORTING_TOP,
	SYSTEMS_SORTING_RECENT,
	SYSTEMS_TYPE_WITH_VERSION,
	SYSTEMS_TYPE_NO_VERSION
}